meta {
  name: user-login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/user/login
  body: json
  auth: inherit
}

params:query {
  foo: bar
}

body:json {
  {
    "username": "your_test_user",
    "password": "your_test_password"
  }
}
